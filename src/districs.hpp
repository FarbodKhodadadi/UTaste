#include <string>
#include <vector>

using namespace std;


class Distirict{
public:
    Distirict(string name_ ,vector<string> neighbours);
    

private:

string name;
vector<string> neighbours;


};